export const featureFlags = {
  // native site pages. redirect to old site until ready.
  nativeWorkPage: false,
  nativeBlogPage: false,
  nativeSnippetsPage: false,
  nativeResumePage: false,
  // Disable redirects by useFeatureRedirect without
  // Having to mess with individual flags above.
  disableFeatureRedirects: true,
}
